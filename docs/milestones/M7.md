# M7: optional AI, pets, maps, polish

Goal: the remaining roadmap items that are each small and independent. Deliver them as separate
commits in the order below so any one can be skipped or shipped alone. Every AI feature is off by
default, uses the admin's own Claude API key, and sends thumbnails only.

Read `CLAUDE.md` first. Expand each item into the M4-style sections at the start of the work.

## Existing seams

- `SettingKeys.aiEnabled`, `aiApiKey` in `apps/server/src/settings.ts` (unused so far).
- Selection sources in `packages/shared/src/book.ts` (`SelectionSource` union) and
  `apps/server/src/immich/gather.ts`; the `smart` source already calls `/search/smart`.
- Chapter openers (`chapter-photo`/`chapter-title`) and the unused `map` template in
  `packages/layout/src/templates.ts`; `planChapters` in `packages/shared/src/chapters.ts`
  yields place keys and GPS clusters per chapter.
- Candidate clusters (near-duplicates) and reasons in the `candidates` table; the review page
  (`apps/web/src/pages/Review.tsx`) shows alternates.
- Captions: `packages/pages/src/captions.ts`; `bookMetaFor` for titles and dates.
- Themes: `packages/shared/src/theme.ts` (`warm-editorial`, `clean-gallery`).
- Templates `colophon` and `qr` exist; shares from M4 provide the link to encode.

## Items

1. **Pets as saved smart queries.** A `pet` source: `{ kind: 'pet', name, query, exampleAssetIds
   [] }` that unions `/search/smart` for `query` and, per example asset, `queryAssetId`
   similarity; a "Pets" section in Settings to save them; wizard support. No new Immich
   permission.
2. **Map pages.** Chapter openers get an optional map slot: an SVG drawn from the chapter's
   photo coordinates (points and a route line) over a plain graticule, no tiles and no network,
   so it renders identically in the PDF. Toggle per book (`rules.chapters` gains `map: boolean`).
3. **QR on the colophon.** Encode the M4 share URL with a small pure-TypeScript QR encoder
   (`packages/layout/src/qr.ts`, unit-tested against known vectors) into the `qr` template;
   the colophon shows the share link text beneath.
4. **Claude features** (all opt-in, key stored encrypted, `docs/ai.md` explains data flow):
   - Captions and chapter titles: `POST /api/books/:id/ai/captions` sends the picked photos'
     `thumbnail` JPEGs with date and place, asks for one-line captions and chapter titles, writes
     them as slot text only where the user has not typed anything. Model id from
     `packages/shared/src/ai.ts` (default the current Claude model at build time), configurable.
   - Best-of-burst: for each cluster with 3+ members, ask which frame is best and apply it as
     the cluster winner with a `reason.kind = 'ai'`; the review page shows it and one click reverts.
   - Foreword: optional title-page paragraph generated from the book's places and dates.
   Rate-limit to one job per book at a time; show token usage and estimated cost in the UI.
5. **Themes.** A third theme and per-book overrides for paper colour, caption font size and
   frame style; the viewer and PDF pick them up through `PageView`.
6. **Notifications.** Optional ntfy/Gotify/webhook URL in settings; fire on render done, order
   status change and selection run failure.

## Judgement calls already made

- No map tiles: offline SVG maps keep the container self-contained and the PDF deterministic.
- AI never sees originals or full-size previews; only Immich `thumbnail` size, and never without
  the toggle.
- Notifications are fire-and-forget with one retry; no queue table.

## Acceptance criteria (per item)

Each item ships with tests for its pure logic, a fake for any external call (a fake Claude
endpoint in `apps/server/src/test/`), docs, and a README roadmap update. The last commit flips the
README status from "early development" to "usable".

## Delivered

Landed 2026-09-15, every item plus the work M4-M6 had deferred, as one commit (the "separate
commits per item" plan was dropped: the items share schemas, Settings and the book page, and were
verified together). Deviations and judgement calls:

1. **Pets.** As specified. Stored as a `pets` setting (JSON, `SavedPet[]`, `PUT /api/settings/pets`),
   edited in Settings and on the new **People & pets** page (the sidebar's placeholder became a real
   page: named people from Immich plus the pets). Several pets picked in the wizard become several
   `pet` sources. The fake Immich answers `queryAssetId` searches by description words, town and hue,
   and every fifth home photo shows "Biscuit the dog".
2. **Map pages.** `rules.chapters` stayed the `ChapterMode` enum; the toggle is a sibling,
   `rules.chapterMaps` (review page and the chapter opener panel in the editor). The map slot sits on
   the `chapter-title` page template (there is no spread template in use). `planMap` in
   `packages/layout/src/map.ts`; points come from the chapter's *placed* photos through
   `chapterMetaFor(book, assets)`.
3. **QR on the colophon.** `paginate` now ends every book with the `colophon` page (`colophon: false`
   opts out; `mergeCustomPages` keeps it last; the editor keeps it last and lets you remove it). The
   encoder supports all 40 versions and four levels and is tested against the standard's
   Reed-Solomon example plus round trips through `jsqr` (dev dependency of `packages/layout`). The
   URL reaches the page as `BookMeta.shareUrl`: renders ask `ShareStore.active` + the configured
   public URL (no public URL = no code, as Settings promised); the editor uses the share list.
   Creating the first active share or revoking the last one bumps `book.updatedAt` (`BookStore.touch`)
   so preflight asks for a new print PDF.
4. **Claude.** Official `@anthropic-ai/sdk` with structured outputs (`messages.parse` +
   `zodOutputFormat`), adaptive thinking at low effort, `AI_BASE_URL` for the fake. Jobs live in a
   new `ai_jobs` table (migration 0005) with usage and estimated cost from a price list in
   `packages/shared/src/ai.ts`. Captions only go on templates with a caption line (grids have none
   by design), so the result reports how many were written and how many the user's own text skipped.
   The foreword got a `foreword` text slot on the title page. Burst picks swap `clusterRank` and
   `decision` and add `ai` reasons pointing at the other frame; revert is `POST .../ai/bursts/revert`.
   `ImageStore.aiThumbnail` is the only image path the AI code can reach (at most 320 px, JPEG q72).
5. **Themes.** `night-gallery` (dark paper) plus `book.themeOverrides` (paper, caption size, frame
   style) merged by `resolveTheme`; `Theme.frameStyle` (none/hairline/shadow/mat) draws on filled,
   non-bleed photo slots in `PageView` and `CoverView`. Edited on the book page's *Look* card.
6. **Notifications.** ntfy (topic URL + optional bearer), Gotify (server + app token) and plain
   JSON webhooks, one retry after two seconds, no queue; per-event switches; fired from
   `RenderService` (done and failed), `OrderService.update` on a status change (after `draft`) and
   `SelectionService` on failure. `apps/server/src/notify/notifier.ts`.

Deferred items from earlier milestones, all done:

- M6: tray photos drag onto a slot (replace) or the page (new box; an automatic page takes the
  automatic route), Shift+click multi-selection (nudge, restack, delete act on all), rotation snaps
  to other boxes' tilt before the 15 degree grid, `cover-safety` preflight warning (text and photo
  boxes inside the front or back safe area, off the spine and wrap). The cover template's title and
  subtitle boxes were narrowed by 8 px / 60 px so the template itself passes.
- M5: Lulu `PRINT_JOB_STATUS_CHANGED` webhooks (subscribe/unsubscribe/test in Settings; signed
  submissions on `/public/lulu/webhook`, HMAC checked against either environment's secret), import of
  print jobs the app does not track (Orders page), and several books per order (`extraBooks`, own
  export URLs and validations per book, one quote, one job with a line item per book). A real Lulu
  sandbox run is still outstanding: no keys were available; the fake covers the whole flow.
- M4: creating a share queues the web preview when none is current, and the viewer polls while a
  render is queued or running ("being prepared") instead of a dead end.

Not done: nothing from the spec. The pay-on-lulu.com link still points at the developer portal's
job list (unverifiable offline).
