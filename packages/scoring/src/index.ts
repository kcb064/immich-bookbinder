// Photo scoring lands in M2: sharpness (Laplacian variance), exposure (histogram), pHash dedupe,
// optional NIMA aesthetic model. Kept as an empty workspace package so the dependency graph is final.
export const SCORING_VERSION = '0.0.0';
